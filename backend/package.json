{
  "name": "lakeloui-se-orchestrator",
  "version": "0.1.0",
  "description": "Mono-Lambda orchestrator for lakeloui.se — EventBridge every 15 min",
  "type": "module",
  "engines": { "node": ">=20" },
  "scripts": {
    "prebuild": "npm install",
    "build": "tsc && for f in dist/*.js; do cp \"$f\" \"${f%.js}.cjs\"; done && node scripts/patch-cjs-requires.mjs && npm prune --omit=dev && cp -r node_modules dist/",
    "invoke": "node dist/handler.js",
    "dry-render": "npm run build && NODE_ENV=development node run_dry_render.cjs",
    "dry-render:4am": "npm run build && NODE_ENV=development REPORT_TYPE=4am node run_dry_render.cjs",
    "dry-render:6am": "npm run build && NODE_ENV=development REPORT_TYPE=6am node run_dry_render.cjs",
    "dry-render:open": "npm run dry-render && (xdg-open /tmp/lakeloui_live_dry_index.html 2>/dev/null || open /tmp/lakeloui_live_dry_index.html 2>/dev/null || echo 'Open file:///tmp/lakeloui_live_dry_index.html in your browser')",
    "check-forecast": "npm run build && node scripts/check-forecast-local.cjs",
    "check-geomet": "npm run build && node scripts/check-geomet-local.cjs",
    "list-geomet-pbl": "node scripts/list-geomet-pbl-capabilities.mjs",
    "check-geomet-precip": "node scripts/check-geomet-precip.mjs"
  },
  "dependencies": {
    "@aws-sdk/client-cloudfront": "^3.975.0",
    "@aws-sdk/client-dynamodb": "^3.975.0",
    "@aws-sdk/client-s3": "^3.975.0",
    "@aws-sdk/client-ses": "^3.975.0",
    "@aws-sdk/lib-dynamodb": "^3.975.0",
    "axios": "^1.13.4",
    "fast-xml-parser": "^5.3.4",
    "netcdfjs": "^3.0.0"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.147",
    "@types/node": "^25.1.0",
    "typescript": "^5.7.2"
  },
  "overrides": {
    "fast-xml-parser": "^5.3.4"
  }
}
