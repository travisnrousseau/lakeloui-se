# WeatherLink v2 API — for station readings in render (Paradise Top + Base).
# Copy to .env and fill in. Get API key/secret from weatherlink.com; station IDs from your dashboard.
# For local dry run: node run_dry_render.cjs (reads .env if present).

WEATHERLINK_API_KEY=wlufqfmxo2ae8guz000r0gh1g2sifmgz
WEATHERLINK_API_SECRET=lqivu5ymhqsi9ng5xlrnbohjojlvg9al
WEATHERLINK_STATION_ID=
WEATHERLINK_STATION_ID_BASE=

# OpenRouter — AI narrative (6am/4am). Leave empty to skip AI; report still renders with groomer fallback.
# OPENROUTER_API_KEY=

# 4am report email — Lambda sends full index.html when run at 4am MST. Set in .env then run infrastructure/terraform/populate-tfvars-from-env.sh and terraform apply.
# REPORT_4AM_EMAIL=you@example.com
# SES_FROM_EMAIL=verified-sender@yourdomain.com

# Cache: dry run uses fixtures/dry-run-cache.json when fresh (no redownload). Models cache 5h (MAX_CACHE_AGE_MODELS=300).
# Set FORCE_FETCH=1 only to refresh; otherwise we reuse cached forecast/msc data and do not redownload GRIB files.
# GRIB_CACHE=0 disables in-run reuse of already-fetched GRIB URLs (default: reuse within same run).
